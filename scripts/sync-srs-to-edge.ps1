# Legacy: SRS now lives in packages/srs and Edge imports it via submit-card-review/deno.json.
# This script is kept only if you need to snapshot-copy for an external deploy; otherwise unused.
Write-Host "SRS source: packages/srs (see supabase/functions/submit-card-review/deno.json)"
